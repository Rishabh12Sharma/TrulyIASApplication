package com.example.trulyiasapplication.Repository

/*
import org.jsoup.Jsoup

class DataRepository(private val apiService: ApiService) {
    suspend fun fetchData(): String {
        val response = apiService.fetchData()
        val htmlContent = response.body()?.htmlContent ?: ""
        val document = Jsoup.parse(htmlContent)
        val parsedData = document.select("your_selector").text() // Use Jsoup to extract data from HTML
        return parsedData
    }
}*/
